apiVersion: v1
kind: Service

metadata:
  name: house-price-api-service

spec:
  type: NodePort

  selector:
    app: house-price-api

  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30080